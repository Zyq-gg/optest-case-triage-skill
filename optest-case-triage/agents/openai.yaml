interface:
  display_name: "Optest Case Triage"
  short_description: "Triage PyTorch optest cases and backfill CSV reports."
  default_prompt: "Use $optest-case-triage to diagnose and document this PyTorch optest case, or safely backfill my CSV summary columns from its authoritative Markdown report."
